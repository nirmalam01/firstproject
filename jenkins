pipeline {
  agent any
       triggers{
         cron('H/15 * * * *')
       }
       stages{
  	stage("Build"){
	   steps{  
	      echo "in build"
	   } 				
	}
      stage("Test"){
           steps{
              echo "in build"
          }
        }
      }
}
