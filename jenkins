pipeline {
  agent any
       stages{
  	stage("Build"){
	   steps{  
	      echo "in build"
	   } 				
	}
      stage("Test"){
           steps{
              echo "in build"
          }
        }
      }
}
